%h1 Your cart

%table.table.table-hover
  %thead
    %tr
      %th Duration
      %th Rental
      %th Price
      
  - @cart.items.each do |item|
    %tr
      %td
        =item.quantity
        = select(:rental, :status, options_for_select([["number of days", "1"], ["pending", "2"], ["success", "3"]]))
      %td
        =image_tag item.rental.image.thumb
        =link_to item.rental.name, item.rental
      %td.text-right=number_to_currency item.total_price, :unit => "€"
    %foot
      %tr
  %td.text-right(colspan=3)
  %strong
  Total:
  =number_to_currency @cart.total_price, :unit => "€"

%p=link_to "Checkout", checkout_cart_path, class: "btn btn-primary"